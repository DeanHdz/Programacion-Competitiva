#include <iostream>
#include <string>
#include <string.h>
#include <vector>
#include <algorithm>
#include <set>
#include <numeric>
#include <map>

#define all(container) begin(container), end(container)

using namespace std;

const int N = 1001;

int main()
{
    
    int t;
    int grid[N][N];
    string output;

    cin >> t;

    for(int i=0; i<t; i++){

    }



}


/*

for(int k = 0; k < MAX; k++){
                int rx=0,ry=0,stopX=0,stopY=0;
                while(rx < maxX  && stopX != 0){
                    
                }
                while(ry < maxY  && stopY != 0){
                    
                }

*/


/*
    //Numero de test cases
    for(int i=0; i<t; i++){
        int n; cin >> n; //Numero de paquetes
        vector<pair<int,int>> grid; //Crear grid vacio

        for(int j=0; j<n; j++){
            int x,y; cin >> x >> y;
            grid.push_back({x,y}); //Recibir e insertar paquetes en grid
            }
        sort(all(grid)); //Acomodar los paquetes en orden
            //Iniciar comprobacion NO
            
        
    }
*/